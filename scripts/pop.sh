#!/bin/bash

sudo apt update && sudo apt upgrade

sudo apt install vim git curl unzip ccrypt keepassxc podman
sudo apt install build-essential libssl-dev

# Já vem com o sistema, colocando apenas para garantir (build-essential curl unzip)

# Vivaldi and vscode by .deb on website
