#!/bin/bash

# Updates
sudo apt update && sudo apt upgrade

# Programs
sudo apt install keepassxc vim git curl

# Rust
sudo apt install build-essential libssl-dev pkg-config

# Git
git config --global credential.helper store

# Dolphin
# Konsole
# Dark theme
